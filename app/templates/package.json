{
  "name": "<%= projectNameSlug %>",
  "description": "<%= projectDesc %>",
  "version": "<%= version %>",
  "private": true,
  "devDependencies": {
    "grunt": "^0.4.2",
    "grunt-contrib-watch": "*",
    "grunt-contrib-connect": "*",
    "grunt-latex": "*"
  }
}
